db:
  mongo:
    host: localhost
    port: 27017
    dbName: null
  mysql:
    host: localhost
    user: root
    password: admin
    database: run
    connectionLimit: 10
server:
  host: localhost
  port: 5000
accessControl:
  - http://localhost:8080